<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundairoad.hyundairoad.member.mapper.MemberMapper">
    <select id="getMemberById" parameterType="Long" resultMap="MemberDTO">
        SELECT member_id, image_id, login_id, email, password, name, nickname, sex, role
        FROM member
        WHERE member_id = #{memberId}
    </select>
    <insert id="signup" parameterType="SignupDTO">
        INSERT
        INTO member(member_id, image_id, login_id, email, password, name, nickname, age, sex, role)
        VALUES (seq_member.nextval, #{imageId}, #{loginId}, #{email}, #{password}, #{name}, #{nickname}, #{age}, #{sex}, #{role})
    </insert>
    <insert id="save" parameterType="Member">
        INSERT
        INTO member(member_id, image_id, login_id, email, password, name, nickname, age, sex, role)
        VALUES (seq_member.nextval, #{imageId}, #{loginId}, #{email}, #{password}, #{name}, #{nickname}, #{age}, #{sex}, #{role})
    </insert>
    <select id="getMemberWithFindPW" parameterType="FindPwDTO" resultType="MemberDTO">
        SELECT member_id, email
        FROM member
        WHERE name = #{name} AND email = #{email}
    </select>
    <select id="getMemberByEmail" resultType="Member">
        SELECT member_id, image_id, login_id, email, password, name, nickname, sex, role
        FROM member
        WHERE email = #{email}
    </select>

</mapper>



