<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundairoad.hyundairoad.image.mapper.ImageMapper">

    <insert id="insertImage">
        INSERT INTO image (image_id, member_id, place_id, course_place_id, saved_name, original_name)
        VALUES (IMAGE_ID_SEQ.NEXTVAL, #{memberId}, #{placeId}, #{coursePlaceId}, #{savedName}, #{originalName})
    </insert>

</mapper>
